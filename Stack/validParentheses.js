/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    //it's just a easy problem, we just use stack to contain the characters, if stack.peek is suitable with s[i] (we loop s) we just pop it out, otherwise we put s[i] in the top of stack
    //if stack is empty after loop, it's valid, otherwise it's not.
    let st = new stack()
};